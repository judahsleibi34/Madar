[Service]
# The leading "+" makes this pre-start command run with full host privileges
# even though the deployment service itself runs as the unprivileged madar user.
# It completes before ExecStart can invoke Docker Compose.
ExecStartPre=+/home/madar/saas/Madar/web/scripts/prepare_production_storage.sh
