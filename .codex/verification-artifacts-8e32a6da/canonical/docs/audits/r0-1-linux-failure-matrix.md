# R0.1 Linux release-blocker failure matrix

Baseline: official backend Linux image and repository mounts, 2026-09-14.
Result before remediation: 1,294 tests, 11 failures, 9 errors.

| Root cause | Exact tests | Expected | Actual | Owner | Correction |
| --- | --- | --- | --- | --- | --- |
| Windows checkout converted Linux executables from tracked LF to CRLF | `test_bridge_promotion_failure_invokes_zero_migration`; `test_ordinary_release_invokes_migration_after_promotion_and_fast_forward` | Wrapper records promotion, fast-forward, and migration ordering; failed promotion invokes no migration | Bash stopped before the fake Git/release tools ran, so `events.log` was never created | `.gitattributes`; `web/deployment/bin/madar-production-deploy` | Enforce LF for executable/parsed release files and normalize the wrapper |
| Same CRLF conversion in backup tooling | `test_backup_dry_run_constructs_commands_without_executing`; `test_backup_is_published_only_after_verified_completion`; `test_backup_requires_environment`; `test_offhost_replication_refuses_an_unmounted_local_directory`; `test_offhost_replication_requires_exact_volume_identity_before_writing`; `test_restore_dry_run_verifies_fixture`; `test_restore_refuses_same_database_and_unsafe_target`; `test_verifier_rejects_missing_member_and_checksum_mismatch` | Exercise backup identity, manifest/checksum, safe restore, mount-root, and volume-identity failures | Every script aborted at line 2 with `set: pipefail: invalid option name`; no invariant ran | `.gitattributes`; `web/scripts/{backup_madar,verify_backup,restore_madar,replicate_backup_offhost}.sh` | Enforce LF and normalize the four scripts; requirements remain unchanged |
| CI image lacked a test-only executable | `test_bundle_attestation_rejects_wrong_missing_and_multiple_heads`; `test_bundle_corruption_after_attestation_fails_before_checkout`; `test_bundle_fetch_failure_cannot_advance_to_checkout`; `test_real_bundle_staging_ignores_replacements_and_hostile_templates`; `test_real_remote_tracking_bundle_stages_exact_detached_candidate` | Execute real template-free Git bundle creation, verification, fetch, and detached checkout | `FileNotFoundError: /usr/bin/git` before bundle behavior ran | `web/backend/Dockerfile`; `.github/workflows/backend-check.yml` | Add Git only to a `test` image target; keep the default production `runtime` target Git-free |
| Stale Compose relative-root assertions | `test_persistent_bind_mounts_keep_their_pre_move_host_paths`; `test_release_deployer_uses_explicit_compose_and_environment_roots`; `test_compose_enforces_worker_resource_and_network_isolation` | Defaults resolve from the explicit `<release>/web` Compose project directory | Tests expected `../.env` and `../backend`; Compose correctly used `.env` and `./backend` | `web/docker-compose.yml`; `web/deployment/bin/madar-release-deploy`; affected tests | Retain the implementation, correct the stale assertions, and add an explicit project-directory regression test |
| Commerce helper fixtures omitted the store-currency dependency | `test_product_accepts_up_to_four_managed_images_from_its_workspace`; `test_product_sku_is_generated_when_left_empty` | Unit-test image ownership and generated SKU without external I/O | `_product_data()` performed the real store-currency Supabase lookup and connection was refused | `web/backend/tests/test_ecommerce_routes.py` | Stub only `_store_currency_for_tenant`; product and currency behavior remain unchanged |

`test_cli_requires_effective_root` was not one of the 20 failures. It passed while
intentionally emitting `effective_root_required`. The test now captures and
asserts that fail-closed diagnostic so it cannot be mistaken for a suite error.
